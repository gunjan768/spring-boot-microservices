#JDBC properties
spring.datasource.url=jdbc:mysql://localhost:3306/employee_directory

#Spring boot will automatically create beans for DataSource and EntityManager and we can inject those into our app
spring.datasource.username=root
spring.datasource.password=
spring.jpa.show-sql=true

# It will everytime create a table and drop the old one (you can use 'update' as a value to update the table and not drop and create table
# at every start)
#spring.jpa.hibernate.ddl-auto=create-drop