package com.example.spring_demo;

// Record is a new Java feature (ctrl + shift + i). Java 15 and higher supports it.
// Record class gives final, getter, setter, public constructor, equals, hashcode, to string
public record Record_Example(String name, int age) {
}